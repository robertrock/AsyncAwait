<Window x:Class="AsyncAwait.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AsyncAwait"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="825">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock FontSize="28" Grid.Row="1" Margin="10" > Simple Async Demo</TextBlock>
        <Button x:Name="executeSync" Content="Normal Execute" Grid.Row="2" Margin="10" Height="25" Click="executeSync_Click" />
        <Button x:Name="executeAsync" Content="Async Execute" Grid.Row="3" Margin="10" Height="25" Click="executeAsync_Click" />
        <TextBlock x:Name="resultsWindow" Grid.Row="4" Margin="10" />

    </Grid>
</Window>
